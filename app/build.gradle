apply from: "${rootProject.rootDir}/config.gradle"

project.ext.ApplicationDefaultConfig(project)

dependencies {
    implementation rootProject.ext.dependencies["android_native"]["appcompat-v7"]
    implementation rootProject.ext.dependencies["android_native"]["constraint-layout"]
}
